### 📝 [ShopMall 프로젝트] 다국어(i18n) 적용 및 빌드 최적화 완료

**📅 작업 일시:** 2026년 5월 12일 16:26

**1. 다국어(i18n) 기능 도입**
* **사용 라이브러리:** `next-intl` (Next.js 15 App Router와 호환성 고려)
* **지원 언어:** 🇰🇷한국어(기본), 🇺🇸영어, 🇯🇵일본어, 🇫🇷프랑스어
* **주요 변경 사항:**
    * 기존의 모든 App Router 페이지를 `app/[locale]/` 디렉토리 하위로 이동하여 언어별 라우팅(`/ko`, `/en` 등) 지원.
    * 헤더(Header), 푸터(Footer), 메인 배너, 장바구니, 마이페이지 등의 UI 텍스트를 각 언어의 JSON 파일(`messages/*.json`)로 분리 및 번역 적용.
    * 우측 상단 헤더에 드롭다운 형태의 **언어 선택기(Language Switcher)** UI 추가.

**2. 도커(Docker) 빌드 속도 및 에러 최적화**
* **빌드 속도 개선 (`.dockerignore` 추가):**
    * 기존 도커 빌드 시 `node_modules`와 `.next` 등 500MB 이상의 불필요한 파일이 도커 엔진으로 전송되며 빌드가 심각하게 지연되던 문제 해결.
    * 프론트엔드 루트에 `.dockerignore`를 생성하여 빌드 컨텍스트 전송 속도를 혁신적으로 단축시켰음.
* **패키지 설치 에러 해결 (`Dockerfile` 수정):**
    * `npm ci` 명령어 사용 시 로컬의 `package-lock.json` 버전과 충돌하여 빌드가 실패하던 문제 확인.
    * `Dockerfile`의 명령어를 `RUN npm install`로 수정하여 호환성 문제 완벽 해결.

**3. 결과**
* 코드를 수정하고 도커 컴포즈(`docker-compose build frontend`)로 재빌드 후 띄우는 것까지 성공적으로 완료.
* 루트 경로 접속 시 로컬 브라우저 언어를 감지하여 알맞은 URL(`/ko` 등)로 자동 리다이렉트 처리 정상 작동 확인.
